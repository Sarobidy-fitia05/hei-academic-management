spring.datasource.url=jdbc:postgresql://ep-summer-feather-b1ekxxab-pooler.c-5.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_9RDB8jeyEafr
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

server.port=8080
server.servlet.context-path=/

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.web=INFO
logging.level.com.example=DEBUG
logging.level.org.springframework.security=DEBUG

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

spring.jackson.serialization.indent_output=true
spring.jackson.time-zone=UTC
spring.jackson.default-property-inclusion=non_null

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT - NE PAS COMMITTER DE SECRET RÉEL DANS LE REPO
# Remplace <VALEUR_ICI> par la valeur réelle via variable d'environnement en prod.
jwt.secret=<VALEUR_ICI>
jwt.expiration-ms=86400000

spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=true

# AWS / storage / events
aws.s3.bucket=hei-academic-management-bucket
aws.eventBridge.bus=hei-academic-management-bus

# Region AWS utilisée par EventBridge / SQS / SES (utilisée par les @Value("${aws.region:...}") que l'on a ajoutées)
aws.region=eu-west-3

# SES configuration (utilisé par la config d'email)
ses.source=noreply@ton-domaine.com
ses.region=eu-west-3

# Optionnel : email de test pour HealthEmailController ou TestEmailRunner
mail.test.to=destinataire@example.com